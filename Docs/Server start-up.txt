Сервер:
31.31.202.41
Логин: root
Пароль: 3%drnymm:#Lk

PHPMyAdmin:
    - URL:      http://31.31.202.41/phpmyadmin/
    - Username: mysqladmin
    - Password: Peigha3eigha

download flywheel.sql
sudo apt install pip
pip install -r requirements.txt
sudo ufw allow 8000
uvicorn api:app --host 0.0.0.0 --port 8000

wait_timeout = 2147483 в /etc/mysql/mysql.conf.d/mysqld.cnf

sudo service apache2 stop
If you've already started the process, and don't want to kill it and restart under nohup, you can send it to the background, then disown it.
Ctrl+Z (suspend the process)
bg (restart the process in the background
disown nnn (assuming this is job #1, use jobs to determine)

Посмотреть список процессов в background
sudo ps -aux 

Мягко убить процесс:
sudo kill -TERM 1024

Добить процесс, если не выгружается
sudo kill -KILL 1024

sudo reboot (при необходимости)

Установка SSL-сертификата
sudo apt install snapd
sudo snap install --classic certbot
sudo certbot certonly --standalone
Сертификаты должны осесть в папке etc/letsencrypt

cd py
Не забыть про другого пользователя базы
uvicorn --reload --ssl-keyfile=privkey.pem --ssl-certfile=fullchain.pem api:app --host 0.0.0.0 --port 8000 >uvicorn.log 2>&1 &

BD: u1669542_amaargiru_wordpress
user: u1669542_amaargi
pass: kU8vW4yI6r

site: Amaargiru
user: amaargiru
pass: a2paPH9sWEqZJpnFZ
email: war4one@gmail.com